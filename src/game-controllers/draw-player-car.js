function drawPlayerCar(
  ctx,
  playerCarImage,
  playerCarXPosition,
  playerCarYPosition,
) {
  ctx.drawImage(playerCarImage, playerCarXPosition, playerCarYPosition);
}

export default drawPlayerCar;
